rules:
  - id: no-eval-usage
    name: Detectar uso de eval()
    message: "Evita usar eval(), puede introducir vulnerabilidades (RCE)."
    severity: ERROR
    languages:
      - javascript
      - typescript
    pattern: eval($X)
    metadata:
      category: security
      cwe: "CWE-95"